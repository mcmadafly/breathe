---
import '../styles/global.css';

import type { AppSession } from '@/lib/auth/session';

import { SiteHeader } from '@/components/site-header';
import { BREATHE_GITHUB_REPO_URL, SITE_DESCRIPTION, TAECHO_SITE_URL } from '@/lib/site';
import { Toaster } from '@/components/ui/sonner';

interface Props {
  title?: string;
  session?: AppSession | null;
  /** When `false`, show an Upgrade control (pass from authenticated routes). */
  isPro?: boolean;
  /** When `false`, hide the sticky site header (e.g. marketing home). */
  showHeader?: boolean;
}

const { title = 'Breathe', session = null, isPro, showHeader = true } = Astro.props;

const skipAuth = import.meta.env.SKIP_AUTH === 'true';
const homeHref = skipAuth ? '/breathe' : session?.user ? '/breathe' : '/';
const showUpgrade = isPro === false;
---

<!doctype html>
<html lang="en" suppressHydrationWarning>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={SITE_DESCRIPTION} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={SITE_DESCRIPTION} />
    <meta property="og:type" content="website" />
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={SITE_DESCRIPTION} />
    <meta name="theme-color" content="#fafafa" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="manifest" href="/manifest.webmanifest" />
    <title>{title}</title>
    <script is:inline>
      (function () {
        const t = localStorage.getItem('theme');
        const dark =
          t === 'dark' ||
          (t !== 'light' && window.matchMedia('(prefers-color-scheme: dark)').matches);
        document.documentElement.classList.toggle('dark', dark);
      })();
    </script>
    <script is:inline>
      if ('serviceWorker' in navigator) {
        window.addEventListener('load', function () {
          navigator.serviceWorker.register('/sw.js');
        });
      }
    </script>
  </head>
  <body class="bg-background text-foreground flex min-h-screen flex-col antialiased">
    {showHeader ? (
      <header
        class="bg-background/80 supports-backdrop-filter:bg-background/60 sticky top-0 z-10 backdrop-blur"
      >
        <SiteHeader
          client:load
          homeHref={homeHref}
          showUpgrade={showUpgrade}
          skipAuth={skipAuth}
          userName={session?.user?.name ?? undefined}
          userEmail={session?.user?.email ?? undefined}
          userImage={session?.user?.image ?? undefined}
        />
      </header>
    ) : null}
    <main
      class:list={[
        'mx-auto w-full max-w-3xl flex-1 px-3',
        showHeader ? 'py-5 sm:py-6' : 'flex flex-col items-center justify-center py-10 sm:py-12',
      ]}
    >
      <slot />
    </main>
    <footer
      class="border-border/80 text-muted-foreground mt-auto border-t py-5 text-center text-xs leading-relaxed"
    >
      <div class="mx-auto max-w-3xl px-3">
        <p>
          Breathe is produced by{' '}
          <a
            href={TAECHO_SITE_URL}
            class="text-foreground font-medium underline decoration-neutral-400/80 underline-offset-2 hover:opacity-90"
            rel="noopener noreferrer"
            target="_blank"
          >Taecho</a>{' '} - an agency for the AI era | {' '}
          <a
            href="/mit-license"
            class="text-foreground font-medium underline decoration-neutral-400/80 underline-offset-2 hover:opacity-90"
          >
            MIT License
          </a> - <a
            href={BREATHE_GITHUB_REPO_URL}
            class="text-foreground font-medium underline decoration-neutral-400/60 underline-offset-2 opacity-60 transition-opacity hover:opacity-100"
            rel="noopener noreferrer"
            target="_blank"
          >
            Contribute on GitHub
          </a>
        </p>
      </div>
    </footer>
    <Toaster client:load />
  </body>
</html>

/** Outbound link for the maker credit in the site footer. Update if Taecho’s canonical URL changes. */
export const TAECHO_SITE_URL = 'https://taecho.co';

export const BREATHE_GITHUB_REPO_URL = 'https://github.com/mcmadafly/breathe';

/** Closing line in the Breathe hover card; included in `SITE_DESCRIPTION` for SEO/social/PWA. */
export const BREATHE_SIGNOFF = 'Breathe. Build. Repeat.';

/**
 * Primary meta description (search, Open Graph, Twitter). Keep `public/manifest.webmanifest` in sync.
 */
export const SITE_DESCRIPTION = `A minimal todo list with room to breathe—capture tasks without clutter. ${BREATHE_SIGNOFF}`;

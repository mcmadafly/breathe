/**
 * Legacy todo `category` column values (pre–todo lists). Still referenced when
 * migrating old rows to `list_id`.
 */
export const LEGACY_CATEGORY_SLUGS = ['work', 'personal', 'home'] as const;
